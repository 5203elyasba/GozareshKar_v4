# سند انتقال و نقشه راه توسعه پروژه

با احترام، طبق درخواست شما، این سند شامل وضعیت فعلی پروژه، لیستی از مشکلات باقی‌مانده به همراه راه حل پیشنهادی، و نقشه راهی برای آینده پروژه می‌باشد. این سند می‌تواند به عنوان نقطه شروع برای یک توسعه‌دهنده یا هوش مصنوعی دیگر استفاده شود.

---

### **بخش اول: مشکلات فعلی و راه حل‌های پیشنهادی**

این موارد باید در **بالاترین اولویت** حل شوند تا برنامه قابل استفاده شود.

**۱. باگ اصلی: خطای `Invalid date parts provided` هنگام ثبت گزارش**
*   **شرح مشکل:** فرم اصلی (`index.php`) تاریخ را در سه بخش مجزای روز، ماه و سال ارسال می‌کند، اما اسکریپت پردازشگر (`submit_log.php`) نمی‌تواند این مقادیر را به درستی دریافت و اعتبارسنجی کند. این باعث می‌شود که هسته اصلی برنامه (ثبت گزارش) کار نکند.
*   **راه حل پیشنهادی:**
    1.  در فایل `submit_log.php`، قبل از هر کاری، باید به طور قطعی بررسی شود که آیا `$_POST['log_day']`، `$_POST['log_month']` و `$_POST['log_year']` وجود دارند (`isset`) و آیا عددی هستند (`is_numeric`).
    2.  پس از اطمینان از وجود این مقادیر، باید آن‌ها را به عدد صحیح (`int`) تبدیل کرده و سپس از تابع `checkdate((int)$month, (int)$day, (int)$year)` برای اعتبارسنجی نهایی استفاده کرد.

**۲. عدم کارکرد دکمه‌های بالا/پایین تاریخ در موبایل**
*   **شرح مشکل:** فیلدهای عددی استاندارد HTML در مرورگرهای موبایل، دکمه‌های کناری (spinner) را نمایش نمی‌دهند.
*   **راه حل پیشنهادی:**
    1.  باید به جای `<input type="number">`، یک کامپوننت سفارشی با HTML ساخته شود. این کامپوننت شامل یک ورودی متنی (`<input type="text">`) و دو دکمه مجزا (`<button>+<button>` و `<button>-<button>`) است.
    2.  با استفاده از جاوا اسکریپت (`main.js`)، باید برای این دکمه‌ها `event listener` تعریف کرد که با هر کلیک، مقدار ورودی متنی را کم یا زیاد کنند.
    3.  ظاهر این کامپوننت باید با CSS طوری طراحی شود که در همه دستگاه‌ها زیبا و کاربردی باشد.

**۳. عدم ذخیره «ساعت ورود» در حافظه مرورگر**
*   **شرح مشکل:** با رفرش کردن صفحه، «ساعت ورود» که کاربر وارد کرده است، پاک می‌شود.
*   **راه حل پیشنهادی:**
    1.  در فایل `main.js`، باید از `localStorage` مرورگر استفاده شود.
    2.  یک `event listener` باید به اولین فیلد «ساعت ورود» اضافه شود که با هر تغییر (`oninput`)، مقدار آن را به همراه تاریخ فعلی در `localStorage` ذخیره کند.
    3.  هنگام بارگذاری صفحه، اسکریپت باید `localStorage` را چک کند. اگر داده‌ای برای همان روز وجود داشت، فیلدهای تاریخ و ساعت ورود را با آن پر کند. اگر تاریخ داده‌ی ذخیره شده برای روز قبل بود، `localStorage` باید پاک شود.

**۴. تغییرات ظاهری و متنی**
*   **پس‌زمینه:** رنگ پس‌زمینه کل صفحه باید `#003e49` باشد.
*   **اجزای شناور:** تمام اجزا مانند فرم‌ها و جداول باید در کارت‌های سفید رنگ با `box-shadow` قرار گیرند تا حس شناور بودن را القا کنند.
*   **تغییر متن:** عبارت «ساعت شروع/پایان» باید در تمام فایل‌ها به «ساعت ورود/خروج» تغییر کند.
*   **منوی راهبری (Navbar):** عبارت «خوش آمدید» باید با «ثبت گزارش روزانه» که به صفحه اصلی لینک شده است، جایگزین شود.

---

### **بخش دوم: فازهای بعدی و آینده پروژه**

این‌ها قابلیت‌های بزرگ‌تری هستند که می‌توان پس از رفع مشکلات بالا، به پروژه اضافه کرد:

**۱. سیستم مدیریت وظایف (مانند میزیتو)**
*   **قابلیت‌ها:** تعریف پروژه، تعریف تسک‌های زیرمجموعه برای هر پروژه، اختصاص تسک به کاربران، تعیین اولویت (فوری، بالا، متوسط)، تعیین تاریخ سررسید، قابلیت ثبت کامنت زیر هر تسک، و نمایش درصد پیشرفت.

**۲. منطق پیشرفته برای روزهای تعطیل**
*   **قابلیت‌ها:** افزودن یک سیستم برای شناسایی تعطیلات رسمی ایران (از طریق یک API یا یک جدول در دیتابیس).
*   محاسبه خودکار این روزهای تعطیل و جمعه‌ها به عنوان روز کاری کامل برای کارمندان در گزارش‌ها.

**۳. نقش‌های کاربری پیشرفته‌تر**
*   **قابلیت‌ها:** اضافه کردن نقش «مدیر بخش» که بتواند فقط گزارش‌های تیم خود را ببیند و برای اعضای تیم خود تسک تعریف کند.

**۴. سیستم اعلان‌ها (Notifications)**
*   **قابلیت‌ها:** ارسال اعلان‌های درون‌برنامه‌ای به کاربران؛ مثلاً هنگام اختصاص یک تسک جدید، نزدیک شدن به مهلت انجام کار، یا ثبت کامنت جدید.

**۵. قابلیت پیوست فایل**
*   **قابلیت‌ها:** امکان آپلود و پیوست کردن فایل (عکس، سند و...) به تسک‌ها و کامنت‌ها.

امیدوارم این نقشه راه به شما در ادامه مسیر توسعه این پروژه کمک کند.
