# برنامه کاری برای افزودن قابلیت‌های جدید

سلام. درخواست‌های جدید شما برای "مدیریت مرخصی" و "داشبورد گزارش‌گیری ادمین" را دریافت کردم. این دو قابلیت بسیار مهم هستند. برای پیاده‌سازی آن‌ها، برنامه کاری زیر را پیشنهاد می‌دهəm:

---

### **فاز ۱: پیاده‌سازی سیستم مدیریت مرخصی**

1.  **تغییرات دیتابیس:**
    *   یک ستون جدید به جدول `users` اضافه می‌کنم به نام `annual_leave_days` تا بتوان تعداد روزهای مرخصی با حقوق سالانه را برای هر کاربر (مثلاً ۲۶ یا ۳۰ روز) ذخیره کرد.
    *   یک جدول جدید به نام `leave_logs` برای ثبت روزهای مرخصی استفاده شده توسط کاربران ایجاد می‌کنم.

2.  **به‌روزرسانی پنل ادمین:**
    *   پنل ادمین را طوری تغییر می‌دهم که مدیر سیستم بتواند تعداد روزهای مرخصی سالانه را برای هر کاربر تنظیم کند.

3.  **ایجاد صفحه ثبت مرخصی:**
    *   یک قابلیت جدید در صفحه اصلی (`index.php`) یا یک صفحه مجزا ایجاد می‌کنم که کاربر بتواند برای یک روز خاص، درخواست مرخصی (با حقوق) ثبت کند.

4.  **نمایش گزارش مرخصی:**
    *   صفحه گزارش‌ها (`reports.php`) را به‌روز می‌کنم تا یک بخش جدید برای نمایش وضعیت مرخصی کاربر داشته باشد، شامل:
        *   تعداد کل روزهای مرخصی استحقاقی
        *   تعداد روزهای استفاده شده
        *   مانده مرخصی

---

### **فاز ۲: پیاده‌سازی داشبورد گزارش‌گیری برای ادمین**

1.  **ایجاد صفحه انتخاب کاربر:**
    *   در پنل ادمین، یک صفحه جدید ایجاد می‌کنم که لیست تمام کارمندان را نمایش دهد.

2.  **نمایش گزارش کارمندان:**
    *   ادمین با کلیک بر روی نام هر کارمند، به صفحه گزارش‌های آن فرد هدایت می‌شود.
    *   صفحه `reports.php` را طوری هوشمند می‌کنم که اگر کاربر وارد شده ادمین بود و یک کارمند دیگر را انتخاب کرده بود، گزارش‌های مربوط به همان کارمند انتخاب شده را نمایش دهد. این گزارش شامل تمام جزئیات و آنالیزهایی است که خود کارمند مشاهده می‌کند.

---

پس از انجام این دو فاز، نسخه جدید و کامل‌تر را برای شما ارسال خواهم کرد.

**اگر با این برنامه موافق هستید، لطفاً تأیید کنید تا کار را شروع کنم.**
