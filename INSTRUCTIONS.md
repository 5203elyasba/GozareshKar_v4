# راهنمای راه‌اندازی و به‌روزرسانی پروژه (نسخه نهایی)

سلام. این راهنما شامل دستورالعمل کامل برای نصب یا به‌روزرسانی پروژه است.

---

### **راهنمای اول: اگر پروژه را برای اولین بار نصب می‌کنید (نصب جدید)**

1.  **کپی کردن فایل‌ها:** پوشه پروژه را در مسیر `htdocs` در XAMPP خود قرار دهید (مثلاً `C:\xampp\htdocs\time-tracker`).
2.  **فعال‌سازی افزونه `intl` در PHP:**
    *   از کنترل پنل XAMPP، کنار **Apache** روی **Config** کلیک کرده و **PHP (php.ini)** را باز کنید.
    *   خط `;extension=intl` را پیدا کرده و نقطه ویرگول (`;`) را از ابتدای آن حذف کنید.
    *   فایل را ذخیره کرده و سرویس **Apache** را **Stop** و سپس **Start** کنید.
3.  **راه‌اندازی دیتابیس:**
    *   وارد `phpMyAdmin` شوید (`http://localhost/phpmyadmin`).
    *   به تب `Import` بروید.
    *   روی `Choose File` کلیک کرده و فایل `database.sql` (از پوشه پروژه) را انتخاب و اجرا کنید. این اسکریپت به صورت خودکار دیتابیس و تمام جداول را می‌سازد.
4.  **اجرای پروژه:** آدرس `http://localhost/time-tracker/login.php` را در مرورگر باز کنید (نام `time-tracker` را به نام پوشه خود تغییر دهید).

---

### **راهنمای دوم: اگر نسخه قبلی را دارید و می‌خواهید به‌روز کنید (به‌روزرسانی)**

1.  **جایگزین کردن فایل‌ها:** تمام فایل‌های پروژه قدیمی را با فایل‌های **نسخه جدید** که دریافت می‌کنید، جایگزین کنید.
2.  **به‌روزرسانی دیتابیس:**
    *   وارد `phpMyAdmin` شوید و دیتابیس `employee_tracker` را انتخاب کنید.
    *   به تب `SQL` بروید.
    *   **محتویات فایل `update.sql` را کپی کرده** و در کادر SQL اجرا کنید. این کار جداول شما را بدون حذف اطلاعات قبلی، به‌روز می‌کند.
    *   **نکته:** اگر با خطای "Duplicate column" یا "Table already exists" مواجه شدید، طبیعی است و می‌توانید آن را نادیده بگیرید.
3.  **اجرای پروژه:** پروژه شما اکنون به‌روز است و می‌توانید از همان آدرس قبلی وارد شوید.

---

### **اطلاعات ورود**
*   **ادمین:** نام کاربری: `admin` / رمز عبور: `password`
*   **کاربر تستی:** نام کاربری: `testuser` / رمز عبور: `password`
